import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  actionsWrapper: {
    position: 'absolute',
    bottom: 30,
    width: '100%'
  },
  rentalTimerWrapper: {
    position: 'absolute',
    top: 0
  }
});
